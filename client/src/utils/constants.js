export const ROUTES = {
  home: "/",
  awareness: "/awareness",
  conditions: "/conditions",
  chatbot: "/chatbot",
  support: "/support",
  dashboard: "/dashboard",
  login: "/login",
  register: "/register",
};

/** JWT only — stored in sessionStorage (not used for dashboard data). */
export const TOKEN_STORAGE_KEY = "mindcare_access_token";
